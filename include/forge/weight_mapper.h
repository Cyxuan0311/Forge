#pragma once

// Weight mapping types are defined in model.h to avoid duplication.
// This header provides the mapping definitions for each architecture.
#include "model.h"

// No additional type definitions here - all types (WeightAlias, 
// LayerWeightMapping, ArchWeightMapping, WeightMapper) are in model.h
